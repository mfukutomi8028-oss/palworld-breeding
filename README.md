# パルワールド配合記録ボード v7

GitHub Pagesで公開できる、Firebase Realtime Database対応の配合記録ボードです。

## v7 修正内容

- 左上・メニューのタマゴ画像を、ユーザー指定の「平凡なタマゴ」画像に差し替えました。
- ブラウザタブのfaviconを、ユーザー指定の公式風パル画像に設定しました。
- ネタバレ防止のため、未登録パルの画像は「？」画像で表示するようにしました。
- 一度でも配合記録に追加されたパルだけ、一覧・詳細・入力候補・絞り込み候補で実画像が表示されます。
- パル名はこれまで通り表示されます。画像のみ伏せます。

## GitHub Pagesへの反映

zipを解凍し、中身をリポジトリ直下へ上書きアップロードしてください。

- index.html
- style.css
- app.js
- config.js
- config.example.js
- firebase-rules.json
- README.md
- assets フォルダ

反映後、公開ページで `Ctrl + F5` を押してください。
古い表示が残る場合は、URL末尾に `?v=7` を付けて確認してください。

## Firebaseについて

`config.js` にFirebase設定を入れると共同編集ONになります。
未設定の場合はブラウザ内のローカル保存で動きます。


## v8 修正内容

- フブキジカの画像が表示されない問題を修正しました。
- 未登録パルの画像は、追加フォーム・絞り込み候補・一覧で「？」画像になるよう再調整しました。
- 一度でも配合記録に登場したパルのみ画像を表示します。
- 登録画面の「確認状況」入力を廃止しました。
- 結果パルが入力されている場合は自動で「配合確認済み」、未入力の場合は自動で「確認中」になります。
- タブアイコンを `favicon-v8.png` に変更し、ブラウザキャッシュを避けるためURLに `?v=8` を付けています。
- 平凡なタマゴ画像を添付画像から切り抜き、`assets/plain-egg.png` として同梱しました。


## v10 修正内容

v9でひらがな検索処理が実ファイルに反映されていなかったため、正規化処理を再修正しました。

- 親A / 親B / 結果パルの候補検索で、ひらがな入力に対応
- 左メニューの親パル / 結果パル絞り込みで、ひらがな入力に対応
- 中央の検索欄で、ひらがな入力に対応
- ひらがな入力のまま保存した場合、パル一覧にある名前はカタカナへ正規化
- `app.js?v=10` にしてブラウザキャッシュを回避
- パル一覧キャッシュキーをv10に更新

例：
- もころん → モコロン
- ふぶきじか → フブキジカ
- らべろっと → ラベロット
- ぺこどん → ペコドン


## v11 修正内容

- 同じ親A・親Bの組み合わせが既に登録済みの場合、新規追加できないようにしました。
- 親Aと親Bが逆でも同じ組み合わせとして判定します。
  - 例：`モコロン + ツッパニャン` と `ツッパニャン + モコロン` は同一扱いです。
- 編集中の同一レコードは重複扱いにせず、そのまま更新できます。
- `app.js?v=11` にしてブラウザキャッシュを回避しています。


## v12 修正内容

- 配合記録に「タマゴの種類」欄を追加しました。
- タマゴ欄はパル選択と同じように、入力しながら候補選択できます。
- 一覧・詳細・候補にタマゴ画像を表示します。
- タマゴ画像は添付画像を `assets/eggs/` に同梱しています。
- 対応タマゴ：
  - 平凡なタマゴ
  - 熱を帯びたタマゴ
  - しめったタマゴ
  - 新緑のタマゴ
  - ビリビリのタマゴ
  - ゴツゴツしたタマゴ
  - 凍てつくタマゴ
  - 暗黒タマゴ
  - 竜のタマゴ
- 重複登録時のエラーを、登録ダイアログ内にも表示するようにしました。
- `app.js?v=12` にしてブラウザキャッシュを回避しています。


## v13 修正内容

- タマゴの種類に「デカ」「キョダイ」を追加しました。
- 画像は通常サイズと同じものを使用します。
- 9種類 × 通常/デカ/キョダイ の合計27種類から選択できます。
- `app.js?v=13` にしてブラウザキャッシュを回避しています。

例：
- 平凡なタマゴ
- デカ平凡なタマゴ
- キョダイ平凡なタマゴ
- 熱を帯びたタマゴ
- デカ熱を帯びたタマゴ
- キョダイ熱を帯びたタマゴ


## v14 修正内容

- タマゴ名の表記をゲーム内表記に合わせて修正しました。
- 誤：`デカ平凡なタマゴ`
- 正：`平凡なデカタマゴ`
- 誤：`キョダイ平凡なタマゴ`
- 正：`平凡なキョダイタマゴ`
- 全9種類のタマゴで同様に修正しています。
- v13で保存済みの誤表記タマゴ名も、候補選択・表示時に正しい名前へ補正されます。
- `app.js?v=14` にしてブラウザキャッシュを回避しています。

例：
- 平凡なタマゴ
- 平凡なデカタマゴ
- 平凡なキョダイタマゴ
- 熱を帯びたタマゴ
- 熱を帯びたデカタマゴ
- 熱を帯びたキョダイタマゴ


## v15 修正内容

- 左上の大きいブランドアイコンを、タブアイコンと同じパル画像に変更しました。
- 左メニュー内の「配合記録」の平凡なタマゴ画像は、そのまま維持しています。
- `app.js?v=15` にしてブラウザキャッシュを回避しています。


## v16 修正内容

- 左メニューの作業適性フィルターで「火おこし」だけボタンが大きくなる問題を修正しました。
- 作業適性ボタンの横幅・余白・文字サイズを調整し、2列で揃うようにしました。
- `app.js?v=16` にしてブラウザキャッシュを回避しています。


## v17 修正内容

- 左上の「配合記録」ブランド画像の枠色を、画像の背景色に近い `#DDC100` へ変更しました。
- 画像自体も少しだけ大きく表示し、枠との色差による違和感を軽減しました。
- 左メニュー内の「配合記録」の平凡なタマゴ画像は変更していません。
- `style.css?v=17` / `app.js?v=17` にしてブラウザキャッシュを回避しています。


## v18 修正内容

- v16の作業適性フィルター調整が、実際のHTMLクラス名と一致していなかったため修正しました。
- `work-filter-group` / `icon-filter-button` に対して直接CSSを当て直し、「火おこし」が折り返されないようにしました。
- 作業適性ボタンの幅・余白・文字サイズを再調整しました。
- `style.css?v=18` / `app.js?v=18` にしてブラウザキャッシュを回避しています。


## v19 修正内容

- 配合記録の詳細に表示される親A・親B・結果パルの名前欄を調整しました。
- 6～7文字程度のパル名ができるだけ1行で表示されるよう、文字サイズ・余白・列幅を調整しました。
- パル名が2行になった場合でも、画像位置がずれにくいようカード内の縦配置を調整しました。
- `style.css?v=19` / `app.js?v=19` にしてブラウザキャッシュを回避しています。


## v20 修正内容

- v19で文字を収めるために変更した詳細欄の縦配置を見直しました。
- パル画像部分はv18以前に近い表示へ戻しました。
- パル名だけを小さめ・文字間狭めにして、6〜7文字程度が1行に収まりやすいよう調整しました。
- `style.css?v=20` / `app.js?v=20` にしてブラウザキャッシュを回避しています。


## v21 修正内容

- 左メニューの作業適性アイコンを大きくしました。
- 火おこしが崩れない範囲で、作業適性ボタンの余白も調整しました。
- 詳細欄のパル画像を少し大きくし、ドロップシャドウを弱めてぼやけにくくしました。
- PalDB画像が使えるパルは、詳細欄の大きい画像ではPalDB側を優先するようにしました。
- `style.css?v=21` / `app.js?v=21` にしてブラウザキャッシュを回避しています。


## v22 修正内容

- パル画像の取得元をPalDB優先に変更しました。
- 起動時に `https://paldb.cc/en/Pals` からパル番号と画像URLを読み取り、既存の日本語パル名データへ紐付けます。
- PalDB画像が取得できたパルは、一覧・候補・詳細でPalDB画像を表示します。
- PalDB取得に失敗した場合は、従来のPalworld Lab画像または内蔵画像へフォールバックします。
- 属性・作業適性アイコンも、夜行性を除いてPalDB CDNの画像URLへ変更しました。
- `style.css?v=22` / `app.js?v=22` にしてブラウザキャッシュを回避しています。


## v23 修正内容

- ユーザー提供の PalDB Breed HTML から、パル番号・PalDB内部キー・画像URLを 214件 抽出し、サイト内の静的マップとして組み込みました。
- `https://paldb.cc/en/Breed` 側の綺麗な画像URLを、日本語パル名へパル番号/内部キー経由で紐づけるようにしました。
- ランタイム取得に失敗しても、この静的マップからPalDB画像を使えるようにしました。
- 既存の `iconKey` があるパルは、番号ズレによる誤表示を避けるため `iconKey` 優先で紐づけます。
- `style.css?v=23` / `app.js?v=23` にしてブラウザキャッシュを回避しています。


## v24 修正内容

- ベノッポなど、PalDB画像URLが存在していても日本語名との紐づけができずPalworld Lab画像へフォールバックしていたパルを、直接PalDB内部キーへ紐づけました。
- 例：`ベノッポ` → `MushroomDragon_Dark`
- `PALDB_JP_ICON_OVERRIDES` を追加し、問題が出やすいパル名は番号推定ではなく日本語名から直接PalDB画像を使います。
- 詳細欄のパル画像にかかっていた影/フィルターを外し、ぼやけにくい表示へ調整しました。
- `style.css?v=24` / `app.js?v=24` にしてブラウザキャッシュを回避しています。


## v25 修正内容

- パル画像の紐づけを、日本語名ではなく共通のパルID（No）優先へ変更しました。
- PalDB Breed由来の静的画像マップから、Noが一致する画像URLを全表示箇所で優先して使います。
- 例：No3 → タマコッコ、No10 → ペンタマ、No104 → リリクイン、No104B → ルナクイン。
- Noで一致しない場合のみ、個別補正・英語名・従来画像へフォールバックします。
- 詳細欄だけでなく、一覧・候補・絞り込みなどパル画像を使う箇所すべてで同じPalDB画像を使います。
- パル画像にかかっていた影/フィルター系の見た目補正を無効化し、ぼやけにくい表示にしました。
- `style.css?v=25` / `app.js?v=25` にしてブラウザキャッシュを回避しています。


## v26 修正内容

- 作業適性アイコンが小さく戻っていたため、再度拡大しました。
- 作業適性アイコンを 22px で表示するよう末尾CSSで上書きしています。
- 「火おこし」が崩れないよう文字サイズも維持しています。
- `style.css?v=26` / `app.js?v=26` にしてブラウザキャッシュを回避しています。


## v27 修正内容

- サイト接続時に記録者を選択する初回ダイアログを追加しました。
- 左メニューのお気に入り下に「現在のユーザー」パネルを追加しました。
- 現在ユーザーは左メニューから福冨/森井に切り替えできます。
- 配合追加・編集時の記録者は、現在選択中のユーザーで自動保存されます。
- 配合追加画面から記録者プルダウンを削除しました。
- お気に入りをユーザー別に分離しました。
  - 福冨のお気に入りと森井のお気に入りは別扱いです。
- 配合一覧の記録者表示をバッジ表示へ変更しました。
- ワールド名入力欄を左メニューへ追加し、タイトル下に表示するようにしました。
- ワールド名はFirebaseの `rooms/{roomId}/meta/worldName` に保存され、同じルームで共有されます。
- ヒーロー背景の推奨作成サイズをREADME/CSSに追記しました。
  - 推奨：1600px × 320px
  - CSS表示：横幅可変、最小高さ210px、角丸28px
- `style.css?v=27` / `app.js?v=27` にしてブラウザキャッシュを回避しています。


## v28 修正内容

- ヒーロー背景画像を、ユーザー提供の `1600px × 320px` 画像へ差し替えました。
- `assets/hero-banner.png` を更新しています。
- 背景画像のキャッシュ対策として `hero-banner.png?v=28` を指定しました。
- `style.css?v=28` / `app.js?v=28` にしてブラウザキャッシュを回避しています。


## v29 修正内容

- v28のヒーロー背景画像差し替えを維持しました。
- お気に入りの登録・解除時に、最終更新日時 `updatedAt` を変更しないように修正しました。
- これにより、更新日時順の並び替えで「お気に入り操作だけした記録」が上に来る問題を防ぎます。
- `style.css?v=29` / `app.js?v=29` にしてブラウザキャッシュを回避しています。


## v30 修正内容

- 重複登録時の表示を改善しました。
  - 「この組み合わせは既に登録済みです」と表示します。
  - 登録画面内に「既存記録を開く」ボタンを表示します。
  - 押すと登録画面を閉じて、既存の該当記録を選択します。
- 配合一覧の親A・親B・結果パルをカード表示にしました。
- 配合記録の詳細でも、親A・親B・結果パルを個別カード表示にしました。
- 結果パルだけ少し大きく表示するようにしました。
- 結果パルの背景を薄いグラデーションカードにしました。
- 配合確認済みの場合、結果パルカードに緑の縁を付けました。
- `style.css?v=30` / `app.js?v=30` / `hero-banner.png?v=30` にしてブラウザキャッシュを回避しています。


## v31 修正内容

- 現在ユーザー欄の右側に、小さなユーザー管理ボタンを追加しました。
- ユーザー管理画面でユーザーの追加・削除ができます。
- 追加したユーザーは、左メニューの記録者プルダウンと初回選択画面に表示されます。
- ユーザー一覧はFirebaseの `rooms/{roomId}/meta/recorders` に保存され、同じルーム内で共有されます。
- 削除しても既存の配合記録の記録者名は残ります。
- 選択中ユーザーを削除した場合は、残っているユーザーへ自動で切り替わります。
- `style.css?v=31` / `app.js?v=31` / `hero-banner.png?v=31` にしてブラウザキャッシュを回避しています。


## v32 修正内容

- 配合記録の詳細画面で、結果パルの画像がカード枠からはみ出して見える問題を修正しました。
- 画像を小さくするのではなく、詳細画面内の親A・親B・結果パルカード枠を広げる方向で調整しました。
- 結果パルカードは少し幅広・高さ高めにして、緑の枠内に画像が収まりやすくしています。
- タマゴの枠色を種類ごとに変更しました。
  - 平凡な：ピンク系
  - 熱を帯びた：炎色
  - しめった：青系
  - 新緑の：緑系
  - ビリビリの：黄色系
  - ゴツゴツした：茶色系
  - 凍てつく：水色系
  - 暗黒：紫系
  - 竜の：濃い紫系
- `style.css?v=32` / `app.js?v=32` / `hero-banner.png?v=32` にしてブラウザキャッシュを回避しています。


## v33 修正内容

- ユーザー管理画面で、ユーザーカラーを設定できるようにしました。
- ユーザー追加時にもカラーを選択できます。
- 登録済みユーザーごとにカラー変更できます。
- 設定したカラーは以下へ反映されます。
  - 現在ユーザーの丸アイコン
  - 配合一覧の記録者バッジ
  - 詳細画面の記録者バッジ
  - ユーザー管理画面のユーザーバッジ
- デフォルトカラーは福冨が青、森井が緑です。
- ユーザーカラーはFirebaseの `rooms/{roomId}/meta/recorderColors` に保存され、同じルーム内で共有されます。
- `style.css?v=33` / `app.js?v=33` / `hero-banner.png?v=33` にしてブラウザキャッシュを回避しています。


## v34 修正内容

- 配合記録の詳細画面で、親A・親Bのパル画像がカード枠から少しはみ出して見える問題を修正しました。
- 画像を小さくするのではなく、親A・親Bカードの幅と高さ、画像枠を少し広げています。
- 結果パルカードはv33の見た目を大きく崩さないよう、必要最小限の幅調整に留めています。
- `style.css?v=34` / `app.js?v=34` / `hero-banner.png?v=34` にしてブラウザキャッシュを回避しています。


## v35 修正内容

- ブラウザのタブに表示されるアイコンを、同じ絵柄のまま角丸にしました。
- ChatGPTのタブアイコンのように、外周の角が丸く見えるよう透過処理を追加しています。
- favicon / shortcut icon / apple-touch-icon を新しい角丸版に差し替えました。


## v36 修正内容

- 配合記録の詳細画面で、親A・親B・結果の3体カードが大枠から飛び出す問題を修正しました。
- 3体カード部分をCSS GridからFlex配置に変更し、大枠内に収まることを優先しました。
- 親A・親Bの画像が右寄りに見えにくいよう、画像枠内の中央配置とカード内の中央揃えを強めました。
- 結果カードの強調は残しつつ、全体が横にはみ出さないようサイズを再調整しました。
- `style.css?v=36` / `app.js?v=36` / `hero-banner.png?v=36` にしてブラウザキャッシュを回避しています。


## v37 修正内容

- タイトル画像エリアから以下を削除しました。
  - `PALWORLD COLLAB BOARD`
  - `リアルタイムで配合結果・確認状況を記録できます。`
  - `共同編集ON`
- ワールド名の表示を目立つように調整しました。
  - 背景付きの大きめピル表示
  - 文字サイズ・余白・コントラストを強化
- `style.css?v=37` / `app.js?v=37` / `hero-banner.png?v=37` にしてブラウザキャッシュを回避しています。


## v38 修正内容

- ヒーロー右上に残っていた `共同編集ON` を完全に非表示にしました。
- `PALWORLD COLLAB BOARD` と説明文の非表示は維持しています。
- タイトル枠の縦幅が小さく見えないよう、ヒーロー枠の高さ・余白を再調整しました。
- ワールド名は横に伸びすぎないコンパクトなバッジ表示へ変更しました。
- `WORLD` ラベル付きにして、目立つが邪魔になりにくい見た目にしています。
- `style.css?v=38` / `app.js?v=38` / `hero-banner.png?v=38` にしてブラウザキャッシュを回避しています。


## v39 修正内容

- PalDBの動的取得元を英語版 `https://paldb.cc/en/Pals` から日本語版 `https://paldb.cc/ja/Pals` に変更しました。
- 現在の画像紐づけはパルNo優先なので、大きな仕様変更はありません。
- ただし、取得元が日本語ページになることで、動的取得時の表示名・補助的な名前照合が日本語寄りになります。
- 既存のPalDB Breed由来の静的画像マップと、パルNo優先の画像表示ロジックは維持しています。
- `style.css?v=39` / `app.js?v=39` / `hero-banner.png?v=39` にしてブラウザキャッシュを回避しています。


## v40 修正内容

- 重複登録時のメッセージから「既存記録を開く」ボタンを削除しました。
- 重複時に既存記録を自動選択する処理も削除しました。
- 今後は登録画面内に以下のメッセージのみ表示します。
  - `この組み合わせは既に登録済みです`
  - `親A ＋ 親B → 結果パル`
- `style.css?v=40` / `app.js?v=40` / `hero-banner.png?v=40` にしてブラウザキャッシュを回避しています。


## v41 修正内容

- フブキジカの画像がツンドラー画像になっていた問題を修正しました。
- `フブキジカ` は日本語名の特殊ケースとして、PalDBの名前別アイコン上書きを `No` より優先するようにしています。
- `style.css?v=41` / `app.js?v=41` でキャッシュ回避しています。


## v42 修正内容

- フブキジカの画像が結果パル候補や一覧でもツンドラー画像になる問題を追加修正しました。
- `palIcon()` 側でも `フブキジカ` の上書き画像を `No` ベースの画像より優先するように変更しました。
- `app.js?v=42` / `style.css?v=42` で再度キャッシュ回避しています。


## v43 修正内容

- `フブキジカ` は実在しない誤登録名だったため、パル一覧データから削除しました。
- `No.083` は `ツンドラー` のみになるよう修正しました。
- 既存データや過去インポートに `フブキジカ` が残っていても、自動的に `ツンドラー` へ読み替えるエイリアス変換を追加しました。
- これにより検索・候補表示・一覧表示・既存記録の読込で `フブキジカ` は出ず、正しく `ツンドラー` として扱われます。
- `style.css?v=43` / `app.js?v=43` でキャッシュ回避しています。


## v44 修正内容

- パル登録画面のタマゴ選択を改善しました。
- 27種類をプルダウンから毎回探すのではなく、以下の2段階で素早く選べるようにしました。
  - 大きさ：通常 / デカ / キョダイ
  - 種類：平凡な / 熱を帯びた / しめった / 新緑の / ビリビリの / ゴツゴツした / 凍てつく / 暗黒 / 竜の
- 種類ボタンにはタマゴ画像を表示しています。
- 選択した大きさと種類から、正式名称（例：平凡なデカタマゴ）を自動入力します。
- 従来どおりテキスト入力・候補検索も使えます。
- `style.css?v=44` / `app.js?v=44` / `hero-banner.png?v=44` にしてブラウザキャッシュを回避しています。


## v45 修正内容

- `クトゥルフのめだま` の画像が No.01 のモコロン画像になる問題を修正しました。
- 原因は、特殊ID `テラ01` が内部処理で `01` と読まれ、PalDBの No.01 と誤って紐づいていたためです。
- 通常のパルNo（例：083 / 104B）のみPalDB画像Noと照合し、`テラ01` のような特殊IDはNo照合から除外するようにしました。
- これにより、クトゥルフのめだまは内蔵の正しい画像URLを使います。
- `style.css?v=45` / `app.js?v=45` / `hero-banner.png?v=45` にしてブラウザキャッシュを回避しています。


## v46 修正内容

- パルワールド正式版1.0でパル総数が増えたことに合わせ、PalDBから取得したパルを候補リストへ自動追加できるようにしました。
- これまではPalDBを主に画像URLの取得元として使っていましたが、v46ではNo・日本語名・画像・属性・作業適性もPalDB側から取り込んで、既存のパル一覧へマージします。
- PalDB側が正式版1.0の新パルに対応していれば、サイト側も再読込だけで新パル候補を取り込める構成にしました。
- PalDBの属性アイコン・作業適性アイコンから属性/作業適性を推定する処理を追加しました。
- 既存のPalworld Lab同期、Firebase、タマゴ選択、ユーザー機能は維持しています。
- `style.css?v=46` / `app.js?v=46` / `hero-banner.png?v=46` にしてブラウザキャッシュを回避しています。


## v47 修正内容（Palworld 1.0 再対応）

- 正式版1.0の追加パルが候補に出なかった問題を根本修正しました。
- 外部サイトのHTML解析だけに依存せず、現行ゲームデータ由来の2つのJSONスナップショットから図鑑を構築します。
  - 日本語名：`palworld-server-tool` の1.0日本語ローカライズ
  - 現行図鑑No・画像アセット：`palworld-icon-manifest.json`（2026-07-15生成、300行対応）
- 現行スナップショット取得に失敗しても、1.0追加分の内蔵フォールバック 76件を即時表示します。
- クインビーナがライバード画像になる問題を修正しました。
  - 1.0で図鑑番号が変動したため、旧Noではなく `iconKey` とパル名を優先して画像を決定します。
  - クインビーナは `QueenBee`、ライバードは `ThunderBird` を明示的に使用します。
- 現行スナップショット取得後は、既存パルを含む現行No・日本語名・画像へ自動更新します。
- `style.css?v=47` / `app.js?v=47` / `hero-banner.png?v=47` にしてキャッシュを回避しています。


## v48 修正内容（1.0図鑑データ再構築）

- v47で追加パルが表示されなかった問題を再修正しました。
- 現行図鑑を以下の3データから構築する方式へ変更しました。
  - 日本語名：1.0日本語ローカライズ
  - 現行図鑑No・属性・作業適性：PalDB 1.0スナップショット（2026-07-15）
  - ゲーム画像アセット：1.0アイコンマニフェスト（300行）
- 取得成功時は250種類以上であることを検証し、不完全なデータは採用しません。
- クインビーナ=`QueenBee`、ライバード=`ThunderBird` を起動時に検証します。
- 画像決定では、1.0で変動した旧図鑑Noより `iconKey` を優先します。
- クインビーナとライバードには明示的な画像上書きも追加しました。
- 外部取得に失敗しても、1.0追加パルの内蔵フォールバックは維持します。
- `style.css?v=48` / `app.js?v=48` / `hero-banner.png?v=48` にしてキャッシュを回避しています。


## v49 修正内容（Palworld正式版1.0 再修正）

- 前回v46で追加パルが候補に出なかった問題を修正しました。
- 1.0追加パル76件を内蔵フォールバックとして起動時に必ず登録します。
- GitHub RawとjsDelivrの2経路から、固定コミットの日本語名・現行図鑑番号・属性・作業適性・画像対応表を同期します。
- 旧Palworld Lab一覧と旧PalDB HTMLキャッシュは、1.0の図鑑番号を上書きするため起動時に読み込まないよう変更しました。
- クインビーナは `No.68 / QueenBee`、ライバードは `No.96 / ThunderBird` としてオフライン時も正しく表示します。
- 画像は旧図鑑Noではなくゲーム内部ID（iconKey）を最優先にしました。
- 起動時に追加パル数とクインビーナ/ライバードの画像対応を検証し、不正な外部データは採用しません。
- `style.css?v=49` / `app.js?v=49` / `hero-banner.png?v=49` でキャッシュを回避しています。
