# パル配合ノート v110 アーキテクチャ

## 目的

ルーム内で実際に確認した配合記録を主機能とし、Palworld 1.0の配合検索・パル図鑑を同じUIとデータマスターで提供する静的Webアプリです。

## 公開構成

- GitHub Pages
- GitHub Actionsによる明示的なPages公開
- HTML / CSS / Vanilla JavaScript
- Firebase Realtime Database
- URLハッシュ `#room=...` によるルーム分離

`.github/workflows/pages.yml` は、v110固定データがまだ存在しない最初の1回だけPalDBから種族値を生成して`main`へ保存します。生成済みの次回実行では固定ファイルだけを使用してGitHub Pagesへ公開します。

## 主要ファイル

- `index.html` — エントリーポイント
- `ui-shell.js` — 画面構造
- `app-core.js` — Firebase、記録正規化、配合マトリクス
- `app-records.js` — 配合記録一覧・詳細
- `app-explore.js` — 配合検索・パル図鑑・種族値表示と並び替え
- `app-review.js` — 確認作業・設定
- `app-data.js` — パル・日本語名・配合・画像・種族値データの独立読込とフォールバック
- `app-actions.js` — UI操作、登録、初期化
- `data/` — 動作確認済みのPalworld 1.0固定データ
- `assets/pals/` — パル299体に対応するローカル画像
- `scripts/build-paldb-stats.py` — PalDB種族値の再現可能な固定データ生成
- `scripts/validate-pal-stats-v110.mjs` — 固定種族値の恒久検証

## Firebase互換性

既存データを維持するため、以下のパスを継続使用します。

- `rooms/{roomId}/records/{recordId}`
- `rooms/{roomId}/meta`

Firebaseの既存記録を移行・一括更新・削除しません。旧レコードの `isMutation` / `mutated`、旧お気に入り形式は読込時に正規化します。

## v102障害の原因

PalDB上で図鑑番号を持たない11体には、番号欄として文字列 `—` が保存されています。v102はこの値を空番号として認識しなかったため、11体すべての内部IDが `—` になりました。

重複ID検査が `289/299` を検出して例外を投げ、パルマスター全体を破棄してFirebase記録名だけの代替一覧へ切り替えた結果、全画像が未確認画像、全図鑑番号が `No.—` になりました。

v103以降では `NULL`、`-`、`—`、`–`、`―` などを未採番として正規化し、内部コードまたは英語名から一意な `special-*` IDを生成します。

## ローカルデータ

サイトの基本機能を外部サービスの一時障害から切り離すため、次の固定データをリポジトリへ保存します。

- `data/pals-v1.json` — パルマスター299件。v110ではHP・攻撃・防御・合計を保持
- `data/pal-stats-v1.json` — PalDB種族値の監査用固定スナップショット299件
- `data/pal-localization-ja-v1.json` — 日本語ローカライズ
- `data/breeding-v1.json` — 299フォーム・特殊配合164件
- `data/pal-images-v1.json` — 画像マニフェスト300行
- `data/SOURCES.md` — 出典、固定コミット、取得日

読込順は、同一オリジンのローカルファイル、固定外部URL、検証済みブラウザキャッシュです。ユーザーのFirebase記録・ルーム設定・お気に入りとは別領域です。

## PalDB種族値

パル図鑑では以下を表示します。

- HP
- 攻撃
- 防御
- 合計（HP＋攻撃＋防御）

攻撃はPalDBの`Attack`、ゲーム内部では`ShotAttack`に相当する種族別係数です。`MeleeAttack`は使用しません。

固定データの生成元は以下です。

1. PalDB `Stats /288` 一覧の通常288体
2. 一覧に含まれない未採番コラボパル11体の個別PalDB `Stats`欄

生成時に次を検証します。

- 288＋11＝299体
- 既存HP・防御がPalDB掲載値と一致すること
- 全件で合計＝HP＋攻撃＋防御
- モコロンがHP70・攻撃70・防御70・合計210

発見記録モードでは、従来どおり発見済みとして表示対象になったパルの種族値だけを確認できます。隠し全パル解放モードでは299体を表示します。

## 必須データと画像の分離

パルマスター、日本語名、図鑑番号、配合エンジンを必須データとして先に構築します。画像マニフェストはその後に独立して読み込みます。

画像データが取得できない場合でも、次は維持されます。

- 299体のパル名
- 299個の一意な内部ID
- 図鑑番号
- 配合検索
- パル図鑑のテキスト情報
- Firebaseの配合記録

画像だけが失敗した場合はローカル代替画像を使用し、配合データ取得失敗として扱いません。

## パル画像

画像の取得順は次のとおりです。

1. `assets/pals/` のローカル画像
2. 固定GitHubコミットの画像
3. 専用CDN
4. `assets/unknown-pal-v8.svg`

PalDB 1.0の実パル299件を画像マニフェストの299行へ番号または英語名で照合しています。余剰1行は外見差分の `Gumoss (Special)` です。

一覧、詳細、登録、配合検索、図鑑、確認作業では同じ画像解決処理を使用します。

## 品質保証

`.github/workflows/validate.yml` で以下を検証します。

- 全JavaScriptの構文
- `index.html` の参照ファイルとv110キャッシュ番号
- ローカルデータと出典文書の存在
- HTML IDの重複とCSS波括弧
- パルマスター299件
- PalDB種族値299件とモコロン70/70/70/210
- 配合フォーム299件・特殊配合164件
- 画像マニフェスト300行
- 全パルIDが一意であること
- 未採番11体がそれぞれ異なるIDを持つこと
- 全299画像のマッピングとローカル画像ファイルの存在
- 種族値のカード・詳細表示
- HP・攻撃・防御・合計の昇順／降順
- 通常のPCブラウザで299体・299画像が読み込めること
- 画像マニフェストを強制遮断しても名称・番号・配合データが維持されること
- スマートフォンで横方向の表示崩れがないこと
- `main`反映後のGitHub Pages実画面
